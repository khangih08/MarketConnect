import React from 'react'

export default function About(){
  return (
    <div>
      <h1 className="text-2xl font-semibold">About</h1>
      <p className="mt-4">Vite + React + TypeScript + Tailwind + Axios setup</p>
    </div>
  )
}
